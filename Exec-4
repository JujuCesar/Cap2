# Entrada de dados
distancia = float(input("Digite a distancia da viagem:\n"))

# Processando dados

if distancia <= 200:
    preco = distancia * 0.50
else:
    preco = distancia * 0.45

# Saída de dados
print(f"O custo total da viagem de {distancia} km é de R${preco}")
