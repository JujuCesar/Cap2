# Entrada de dados
numero = int(input("Digite um numero que deseja calcular:\n"))

# Entrando com o intervalo
inicio = int(input("Digite o inicio do intervalo:\n"))
fim = int(input("Digite o fim do intervalo:\n"))

if inicio > fim: # Garante que a saída de dados sera crescente
    inicio, fim = fim, inicio

# Saída de dados
print(f"Tabuada do {numero} de {inicio} até {fim}\n")

for i in range(inicio,fim +1): # O "+ 1" garante que o ultimo numero será lido pelo range
    print(f"{numero} x {i} = {numero * i}")
